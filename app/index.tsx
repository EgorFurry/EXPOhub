import { Text, View } from 'react-native';

export default function Index() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0B0B12', alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#fff' }}>ExpoHub работает!</Text>
    </View>
  );
}